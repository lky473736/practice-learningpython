# 정수 나눗셈 : // (값 중 정수만 출력)
# 실수 나눗셈 : / (값을 전부 표현)
# 할당연산 : 한번에 여러 개의 변수에 값을 할당할 수 있다.
# example) x, y, z = 3, 4, 5
# example) x, y = y, x (x와 y의 저장되어 있는 값을 교환한다 => 파이썬에서만 가능하다 (다른 언어에서는 temp를 사용))
# 복합연산자 : +=와 같이 연산자 두개가 나란히 복합된 연산자 : 기존의 연산자 의미와 다른 의미를 가진다
# sum = sum + i ==> sum += i
# sum = sum * i ==> sum *= i
# sum = sum - i ==> sum -= i
# sum = sum / i ==> sum /= i
'''
x = 1000
print ("초기값 : ", x)
 
x += 2
print ("+= 2 한 값 : ", x)

x -= 2
print ("-= 2 한 값 = ", x)
'''

# Pass.